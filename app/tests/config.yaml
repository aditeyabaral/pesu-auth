username: "${USERNAME}"
password: "${PASSWORD}"
name: "${NAME}"
prn: "${PRN}"
srn: "${SRN}"
program: "${PROGRAM}"
branch_short_code: "${BRANCH_SHORT_CODE}"
branch: "${BRANCH}"
semester: "${SEMESTER}"
section: "${SECTION}"
email: "${EMAIL}"
phone: "${PHONE}"
campus_code: "${CAMPUS_CODE}"
campus: "${CAMPUS}"
cycle: "${CYCLE}"

